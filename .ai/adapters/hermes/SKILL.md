---
name: project-brain
 description: Use canonical project-local Project Brain context safely.
---
# Project Brain

Run `./ai start`, read only the generated brief and relevant canonical files under `.ai/`, preserve unknown facts, and run `./ai check` after changes. This entry is project-local; never modify `~/.hermes`.
